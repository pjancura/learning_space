// data types
// int  = whole numbers

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;                               // example of an integer, whole number
    double gpa = 3.7;                           // double and float are both able to hold decimal numbers
    char grade = 'B';                           // only stores a single character and uses single quotation marks
    char phrase[] = "Giraffe Academy";          // this creates a string, or an array of characters


    return 0;
}